// src/app/page.tsx
import KnowRahWidget from "@/components/KnowRahWidget";

export default function HomePage() {
  // The layout provides the container, header, and footer.
  // We render just the widget here.
  return <KnowRahWidget />;
}
